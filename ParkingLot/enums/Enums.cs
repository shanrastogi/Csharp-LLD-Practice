namespace enums
{
    public enum GateType { Entry, Exit }
    public enum PaymentMode { Cash, Upi, Card }
    public enum PaymentStatus { Pending, Success, Failed }
    public enum PricingStrategyType { FlatRate, HourlyRate }
    public enum VehicleType { Car, Bike, Truck }
}